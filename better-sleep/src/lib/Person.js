class Person{

    name

    /**
     * Constructs a Person instance
     * @param {string} name Name of the Person
     */
    constructor(name) {
        this.name = name
    }

}

module.exports = Person
